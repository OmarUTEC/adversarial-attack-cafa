attack_name: jsma

theta: 0.1
gamma: 1.0
batch_size: 128
verbose: true

# Tabular constraints injected by the pipeline wrapper
feature_clip_min:
feature_clip_max:
integer_indices:
categorical_groups:
editable_mask:
only_increase_mask:
only_decrease_mask:
max_abs_step:
